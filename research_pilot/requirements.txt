# ==============================================================================
# ResearchPilot API - Python Dependencies Manifest
# ==============================================================================
# Node.js Comparison: Equivalent to `package.json` dependencies.
# Exact versions ensure reproducible builds across development and production.
# ==============================================================================

# Core Web Framework & Server (ASGI engine vs. Node's event loop)
fastapi>=0.111.0,<0.112.0
uvicorn[standard]>=0.30.0,<0.31.0

# Settings & Data Validation (Pydantic v2 replaces runtime JS validation tools like Zod/Joi)
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0

# Database & ORM (SQLAlchemy 2.0 async ORM replaces Prisma/Sequelize)
sqlalchemy>=2.0.30,<2.1.0
asyncpg>=0.29.0,<0.30.0

# Vector Database Client
chromadb>=0.5.0

# AI Frameworks (LangChain & LangGraph for agent orchestration)
langchain>=0.2.0
langchain-core>=0.2.0
langgraph>=0.0.50

# Asynchronous Task Queue & Cache (Celery & Redis replaces BullMQ / Bee-Queue in Node)
celery>=5.4.0
redis>=5.0.0
